import * as React from 'react';
import {StatusBar,ImageBackground, StyleSheet, Alert,Text, View, Image, TouchableOpacity } from 'react-native';
import {Appbar, } from 'react-native-paper';
import styles from '../styles/styles.js';
const RecentsRoute = () => <ImageBackground source={require('../assets/frame.png')} style={styles.dailymatchbakcgroundImage}>
      
 </ImageBackground> ;

  export default RecentsRoute;